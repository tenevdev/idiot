'use strict';

/**
 * @ngdoc function
 * @name idiotApp.controller:ExploreCtrl
 * @description
 * # ExploreCtrl
 * Controller of the idiotApp
 */
angular.module('idiotApp')
  .controller('ExploreCtrl', function ($scope, $http) {
    
  //   $http.post('/api/', {msg:'sup!'})
  //   .success(function(data, status, headers, config) {

  // })
  // .error(function(data, status, headers, config) {
  
  // });

  });

