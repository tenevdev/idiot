'use strict';

/**
 * @ngdoc function
 * @name idiotApp.controller:SigninCtrl
 * @description
 * # SigninCtrl
 * Controller of the idiotApp
 */
angular.module('idiotApp')
  .controller('SigninCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
